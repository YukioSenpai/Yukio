import { stylesheet } from 'typestyle'

export const css = stylesheet({
    container: {
        width: '100vw',
        height: '5rem',
        background: 'black',
        color: 'white',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-evenly'
    }
})